# 2026 RoboCup 无人机快递赛：自主搜索、精准投放、过门与降落技术方案

版本：V1.0  
日期：2026-07-28  
适用系统：MID-360 + FAST-LIO2 + EGO-Planner + 45°下视全局快门相机 + YOLO/传统视觉 + 三路独立 PWM 重力投放机构

> 本文是比赛执行基线，不是未经验证即可承诺成功的“理论完美路线”。所有规则硬约束、软件状态机、默认参数、失效处置和验收试验均在本文中闭环；只有第 19 章验收项全部通过，系统才可进入正式比赛配置。

## 0. 执行摘要

### 0.1 总体策略

1. 起飞后由 FAST-LIO2 持续提供主里程计，MID-360 建图，EGO-Planner 执行局部避障与轨迹生成。
2. 不依赖固定起飞坐标，也不把 2025 场地图上的靶位当作 2026 坐标。系统在线估计场地长轴、投放区边界和自由空间，以当前位置生成分段“往复式”覆盖路线。
3. 搜索高度固定为 1.4 m。45°相机采用可靠中央 ROI；推荐航带间距 1.1 m，利用可见性栅格记录“真正看见”的地面，而不是只记录飞过的路径。
4. YOLO 负责类别和 ROI，传统几何负责特征点，平面靶优先使用 Homography/RANSAC + IPPE PnP，融合 LIO 位姿得到靶标世界坐标。
5. 三个载荷按“红十字 + 两个最高价值标准靶”使用。默认价值顺序为红十字、装甲车、桥梁、地堡、帐篷；红十字未确认前保留一个可靠舵机通道。
6. 高优先级目标满足分类稳定、世界位姿收敛、下降走廊安全和时间预算时立即插入投放任务。投放后不论物理结果是否可见，都将该目标标记为“已尝试、释放未验证”，不重复投放。
7. 投放接近阶段先由 EGO 导航到视觉斜距点，视觉更新目标参考，FAST-LIO2 始终提供机体状态。目标从图像下边缘按预期消失后，仅允许 0.1–0.2 m、最长 1 s 的 LIO 终端桥接，再悬停并开舵机。
8. 完成投放或达到时间截止后，MID-360 拟合门框，通过两扇约 0.8 m 的可调门，最后对 H 降落板进行视觉精对准并自主降落。

### 0.2 比赛成功的三条硬线

- 不以历史坐标替代搜索，不因识别到三个目标而遗漏过门和降落时间。
- 不因单帧丢失目标进入盲降；预期丢失、异常丢失必须分支处理。
- 普通 PWM 舵机没有物理反馈，软件只能报告 `EMPTY_ASSUMED`，不得报告“已确认释放”。

## 1. 规则约束与设计边界

### 1.1 2026 规则硬约束

| 项目 | 规则事实 | 对设计的直接影响 |
|---|---|---|
| 场地 | 外尺寸约 10 m × 10 m × 4 m | 所有几何参数化，不能硬编码 2025 的 10 m × 8 m |
| 投放区 | 4 个 1 m × 1 m 标准靶 + 1 个 0.35 m × 0.35 m 红十字随机靶 | 小靶需要更严格定位和更低投放高度 |
| 障碍 | 4 棵模拟树、4 个边长 0.8 m 纸箱 | 1.4 m 航线仍必须在线绕行并处理视觉遮挡 |
| 围墙/走廊 | 外围及内部墙高 1.5 m；走廊宽 1.5 m；两道墙各有约 0.8 m 可调门 | 不能靠越顶规避；门洞必须在线测量 |
| 随机性 | 选手停止电脑/遥控操作后，裁判调整靶标和障碍位置 | 正式飞行不能依赖人工更新地图或目标坐标 |
| 飞行 | 全程自主；单次飞行不超过 10 min；高度不超过 4 m | 任务管理器必须有硬截止和保留时间 |
| 碰撞 | 撞墙、地面、网等可能终止；接触树木会损失避障分 | 安全监督器优先于目标得分 |
| 避障 | 在线规划水平越过投放区障碍且无碰撞得分；从障碍上方翻越或全程高于障碍物不得分 | 搜索路线应穿越自由通道，不采用高空直飞 |
| 投放 | 目标落点按环和类别权重计分 | 优先级由期望得分、成功率和绕行代价共同决定 |
| 过门 | 每扇门无碰撞 15 分，有碰撞通过 10 分，最多 30 分 | 为过门和降落保留 120–150 s |
| 降落 | H 区自主降落 10 分，压边 5 分 | 采用与投放共用的视觉/LIO精对准框架 |

### 1.2 计分导出的载荷优先级

若货物落入靶心内环，类别的最大投放价值为：

| 类别 | 权重 | 单个最大分值 | 默认投放顺序 |
|---|---:|---:|---:|
| 红十字随机靶 | 10 | 50 | 1 |
| 装甲车 | 2.5 | 12.5 | 2 |
| 桥梁 | 2 | 10 | 3 |
| 地堡 | 1.5 | 7.5 | 4 |
| 帐篷 | 1 | 5 | 5 |

三件载荷的理论最优组合是红十字、装甲车、桥梁，投放满分 72.5。实际任务选择使用期望效用：

`U = P_class × P_drop × score − λd × detour_time − λr × risk − λt × deadline_pressure`

其中 `P_class` 是类别置信度，`P_drop` 是基于定位方差、投放高度和风扰估计的命中概率。

### 1.3 待裁判确认项

2026 规则的标准靶图案说明出现“坦克”，但计分权重列表没有坦克。赛前领队会必须书面确认其计分映射。在确认前：

- YOLO 保留 `TANK_UNKNOWN` 类；
- 不将其加入前三优先级；
- 不因识别到该类而消耗载荷；
- 配置文件中的 `tank_weight` 必须为 `null`，任务启动门禁检查其状态。

### 1.4 假设和可调参数

- 树木高度按约 1.5 m 做安全建模，但以 MID-360 实测点云为准。
- 2025 场地图只用于说明路线形态和测试场搭建，不用于 2026 正式比赛坐标。
- 45°为相机光轴相对水平面的下倾角；100°暂按水平视场角计算，最终以标定内参为准。
- 文中速度、阈值和时间均是首轮标定值，必须由第 19 章试验回归后冻结。

## 2. 系统总体架构

### 2.1 责任分层

| 层 | 模块 | 唯一职责 |
|---|---|---|
| 感知层 | MID-360、相机、YOLO、几何特征、PnP | 输出障碍点云、目标类别、目标相机位姿和质量 |
| 状态估计层 | FAST-LIO2 | 连续输出 `T_map_body`、速度、协方差和健康状态 |
| 世界模型层 | 占据图、可见性图、目标数据库 | 统一保存可飞空间、搜索完成度、目标世界状态 |
| 规划层 | EGO-Planner、覆盖规划器、终端轨迹器 | 输出无碰轨迹、搜索段和短距离终端桥接 |
| 任务层 | Mission Manager、双优先队列、时间管理器 | 决定“继续搜索、插入投放、进走廊或降落” |
| 执行层 | 飞控、三路 PWM 舵机 | 跟踪轨迹并执行单次释放 |
| 安全层 | Safety Supervisor | 对所有命令做健康、碰撞、姿态和时间门禁 |

### 2.2 建议软件节点

- `mission_manager`：顶层有限状态机和任务仲裁。
- `field_frame_estimator`：在线估计场地边界、长轴和投放区多边形。
- `coverage_planner`：生成/修补搜索段，维护可见性栅格。
- `target_perception`：YOLO、ROI 几何、PnP 和质量评估。
- `target_tracker`：数据关联、世界位姿滤波、重复目标合并。
- `precision_approach`：斜距接近与视觉引导下降。
- `terminal_bridge`：预期丢失后的短轨迹。
- `payload_manager`：三个舵机的锁存状态和事务。
- `corridor_manager`：门框拟合、门中心跟踪和穿门轨迹。
- `safety_supervisor`：LIO、地图、碰撞、姿态、超时和急停策略。
- `mission_logger`：记录图像、点云、里程计、决策、PWM 和评分证据。

FAST-LIO2 是整个飞行期间的主状态估计，不在视觉接管时关闭；视觉给的是目标参考修正，不是替代里程计。

## 3. 坐标系、标定与时间同步

### 3.1 坐标系

- `map`：FAST-LIO2 局部世界系，正式比赛无需绝对地理坐标。
- `body`：无人机机体系。
- `lidar`：MID-360 坐标系。
- `camera`：相机光学坐标系。
- `target_i`：第 i 个靶标模板坐标系，原点设在靶心。
- `field`：在线估计的场地坐标系，x 轴沿投放区长轴。

目标世界位姿统一使用：

`T_map_target = T_map_body(t_img) · T_body_camera · T_camera_target`

必须把 LIO 位姿插值到图像曝光时刻 `t_img`，不能直接使用“最新里程计”。

### 3.2 必做标定

1. 相机内参和畸变：覆盖全画面、不同距离和姿态，保存分辨率绑定的 `K,D`。
2. 相机—机体外参：45°固定支架安装完成后标定，拆装后重做。
3. MID-360—机体外参：与飞控重心/控制坐标一致。
4. 雷达—相机外参：用于点云投影、遮挡判断和安全下降圆柱检查。
5. 时间同步：硬件触发优先；否则测量并补偿相机、雷达、IMU 和计算机时钟偏差。
6. 三路舵机：分别标定锁止脉宽、释放脉宽、保持时间、负载下动作时间。

### 3.3 启动健康门禁

只有同时满足以下条件，`READY` 才允许转入 `TAKEOFF`：

- FAST-LIO2 初始化完成，协方差低于上限，IMU 无饱和；
- MID-360 点云频率与时间戳正常；
- 相机帧率、曝光、标定分辨率匹配；
- 三路舵机均在锁止 PWM，软件状态均为 `LOADED_ASSUMED`；
- 配置文件版本、规则版本、目标模板版本一致；
- `TANK_UNKNOWN` 的处理策略已按领队会结果配置；
- 记录磁盘空间、电池电压和任务时钟正常。

## 4. 顶层任务状态机

### 4.1 主状态

`BOOT → SELF_CHECK → READY → TAKEOFF → FIELD_INIT → SEARCH_NAV ↔ OPPORTUNITY_DROP → CORRIDOR_ENTRY → DOOR_1 → DOOR_2 → LAND_APPROACH → LAND → COMPLETE`

任意状态可转入 `SAFE_HOVER`、`ABORT_RETURN` 或 `EMERGENCY_STOP`。

### 4.2 关键转移

| 当前状态 | 条件 | 下一状态 | 动作 |
|---|---|---|---|
| TAKEOFF | 高度 >1 m 且稳定 10 s | FIELD_INIT | 建立地图、估计场地轴 |
| FIELD_INIT | 得到可用自由空间和搜索方向 | SEARCH_NAV | 生成最近端点优先的首批搜索段 |
| SEARCH_NAV | 高优先目标可投且满足全部门禁 | OPPORTUNITY_DROP | 保存搜索检查点，锁定目标与舵机 |
| OPPORTUNITY_DROP | 投放事务结束 | SEARCH_NAV | 升至 1.4 m，从前向安全点重入 |
| SEARCH_NAV | 三件载荷均尝试；或覆盖完成；或时间截止 | CORRIDOR_ENTRY | 冻结投放队列，规划走廊入口 |
| 任意飞行状态 | 剩余时间小于安全返航预算 | CORRIDOR_ENTRY/ABORT_RETURN | 放弃低价值任务 |
| DOOR_2 | 穿门完成 | LAND_APPROACH | 搜索并跟踪 H |
| LAND_APPROACH | 视觉/LIO终端下降完成 | LAND | 触地检测、停桨 |

### 4.3 仲裁优先级

`安全约束 > 规则时间 > 过门/降落保底 > 红十字 > 装甲车 > 桥梁 > 继续覆盖 > 低价值投放`

## 5. 起点未知时的场地初始化

### 5.1 不依赖绝对起点

FAST-LIO2 的 `map` 原点可以在任意起飞位置。任务规划只使用相对几何：

1. 起飞至 1.4 m 并原地慢速偏航扫描，建立局部点云。
2. 从垂直平面和地面边界中拟合场地墙线。
3. 对主要平行墙方向做 RANSAC/PCA，定义 `field.x` 长轴。
4. 从已知拓扑识别投放区与狭长走廊；若尚未确定，先生成局部安全探索段。
5. 将当前自由空间切成可执行搜索单元，每个搜索段有两个端点。
6. 首段选择代价最小的安全端点，而不是假设从左下角开始。

端点代价：

`C_endpoint = path_length + λturn × yaw_change + λrisk × obstacle_cost − λprior × historical_soft_prior`

历史靶位只可影响段顺序的轻微权重，绝不能把未覆盖区域标记为已搜索。

### 5.2 初始化失败回退

- 墙线不足：以当前位置为中心执行低速扩展矩形探索，直到形成可用边界。
- 场地轴多解：选择与最大自由空间主方向一致的轴，并保留 90°备选。
- 投放区/走廊未分离：先搜索面积较大的自由区，持续更新拓扑；不得进入窄通道。

## 6. 1.4 m 搜索几何与覆盖路线

### 6.1 相机几何

按水平 FOV 100°、16:9 和光轴下倾 45°估算：

- 垂直 FOV 约 67.7°；
- 1.4 m 高度的理论地面前缘约 0.28 m，远缘约 7.09 m；
- 光轴中心所在距离约 1.4 m，理论中心行地面宽度约 4.72 m；
- 为抑制边缘畸变、远端像素不足和遮挡，只使用水平 ±35°、垂直 ±20°的可靠 ROI；
- 可靠地面纵向约 0.65–3.00 m，中心附近有效宽度约 2.77 m；
- 推荐航带中心距 1.1 m，标定充分后最多放宽至 1.2 m。

0.35 m 红十字在中心行的理论宽度约为：1920 像素图像 142 px、1280 图像 95 px、640 图像 48 px。最终检测下限必须用真实相机、真实曝光和运动模糊测试确定。

### 6.2 路线生成

1. 将投放区自由地面投影为二维多边形。
2. 沿 `field.x` 长轴按 1.1 m 间距生成平行航带。
3. 用膨胀后的占据图裁剪航带，分割成若干安全段。
4. 按当前位姿、转弯代价、未覆盖增益和软先验选择下一段。
5. 段内速度 0.5–0.7 m/s；近障碍降至 0.25–0.4 m/s。
6. 转弯使用 EGO-Planner 的平滑轨迹，禁止贴墙急转。

### 6.3 可见性栅格

使用 0.1 m 栅格维护：

- `UNKNOWN`：未被可靠观察；
- `VISIBLE_FREE`：可靠 ROI 内、无点云遮挡、图像质量合格；
- `OCCLUDED`：被树、箱或墙遮挡；
- `LOW_QUALITY`：模糊、过曝、欠曝或目标像素不足；
- `NO_FLY`：障碍膨胀区。

只有从相机模型投影到可靠 ROI、通过点云射线遮挡检查且图像清晰度达标时，栅格才记为已覆盖。不能用无人机轨迹的扫过面积替代视觉覆盖。

### 6.4 遮挡修补

1. 对障碍物背后的 `OCCLUDED` 连通域生成侧视候选点。
2. 至少从相反的两个方位观察树木/纸箱周边。
3. 选择信息增益/路程比最高且 EGO 可达的修补点。
4. 障碍顶部约 1.5 m，而飞行高度 1.4 m，因此默认按完全遮挡处理，禁止寄希望于越顶看见靶标。

### 6.5 搜索完成条件

满足任一强制条件即结束搜索：

- 5 个靶标均已稳定建档，且有效自由地面覆盖率 ≥98%，不存在大于 0.25 m × 0.25 m 的未解释盲区；
- 三件载荷均已尝试，且已满足非走廊避障遍历要求；
- 时间管理器触发截止；
- 安全监督器要求转入返航/走廊。

## 7. 目标检测、几何定位与世界建档

### 7.1 两级视觉链

1. YOLO 输出类别、置信度、ROI 和初步方向。
2. 按类别调用几何提取器：
   - 装甲车：利用图案内黑条构成的 6 个唯一、有序、分布良好且不共线的模板点；
   - H 起降板：优先使用外框角点和 H 的结构约束；
   - 红十字：十字凹凸顶点、外板角点和外圆/椭圆联合约束；
   - 其他标准靶：外方框/圆环和模板关键点联合匹配。
3. 特征点先从 letterbox/缩放后的 ROI 坐标反变换到完整原图，再去畸变和 PnP。

Harris 角点只能生成候选点；没有模板语义和顺序约束的角点不能直接送入 PnP。

### 7.2 平面 PnP

所有靶面近似共面，推荐流程：

1. 亚像素角点/边缘精化；
2. Homography + RANSAC 去除外点；
3. `solvePnPGeneric(..., SOLVEPNP_IPPE)`；方形外框可使用 `IPPE_SQUARE`；
4. 对多解检查正深度、法向方向、重投影误差和时间连续性；
5. 计算目标世界坐标和协方差。

拒绝条件建议初值：

- 有效点数小于类别要求；
- 重投影 RMSE >2.5 px（红十字建议 >2.0 px 拒绝）；
- PnP 深度/尺度与 LIO 高度不一致超过 15%；
- 连续帧世界位置跳变 >0.20 m；
- 平面法向与地面法向夹角异常；
- 目标处于可靠 ROI 外或运动模糊超阈值。

### 7.3 目标记录

每个目标保存：

```text
target_id, class_id, class_probability
T_map_target, covariance_6x6
reprojection_rmse, inlier_count
first_seen, last_seen, seen_count
approach_pose, descent_corridor_clear
status: CANDIDATE | CONFIRMED | RESERVED | ATTEMPTED | RELEASED_UNVERIFIED | SKIPPED
```

目标进入 `CONFIRMED` 的建议条件：至少 5 帧有效观测、时间跨度 ≥0.3 s、类别多数投票一致、位置标准差标准靶 <8 cm、红十字 <4 cm。

### 7.4 重复目标合并

同时满足世界距离、类别和外观一致性才合并：

- 标准靶中心距离 <0.45 m；
- 红十字中心距离 <0.20 m；
- 马氏距离低于门限；
- 同一时间不可能对应两个相距明显的观测。

已经 `ATTEMPTED` 的目标永不因新检测重新回到可投队列。

## 8. 双队列与机会投放决策

### 8.1 搜索队列 `Q_search`

元素包含：

```text
segment_id, start_pose, end_pose
coverage_gain, obstacle_risk, traversal_cost
checkpoint_s, status, historical_soft_prior
```

每 0.5–1.0 s 重排尚未执行的段；当前段除非安全或高价值投放触发，不频繁抢占。

### 8.2 投放队列 `Q_drop`

元素包含：

```text
target_id, class_weight, expected_score
pose_covariance, classification_stability
detour_time, descent_risk, deadline
reserved_payload_channel, utility
```

### 8.3 直接插入投放的全部门禁

- 目标属于当前前三候选：红十字、装甲车、桥梁；或时间截止后的最佳后备目标；
- 目标状态为 `CONFIRMED`，连续观测和 PnP 质量达标；
- 下降圆柱和斜进近通道无障碍；
- 至少一个舵机通道为 `LOADED_ASSUMED`；
- 红十字尚未发现时，非红十字投放后仍保留一个载荷；
- 估计投放耗时后仍满足走廊/降落保留时间；
- FAST-LIO2、局部地图、相机和飞控健康；
- 当前飞行段可安全中断，并已写入搜索检查点。

### 8.4 时间管理

默认估计：

- 每个剩余投放：45–60 s；
- 走廊入口、两扇门和降落：保留 120–150 s；
- 安全裕度：30 s。

若：

`remaining_time < remaining_payloads × drop_estimate + corridor_landing_reserve + safety_margin`

则停止扩大搜索，依次投放已知最高效用目标；若连完成走廊/降落都不足，则跳过低价值投放直接返航。

## 9. 精准接近与视觉引导下降

### 9.1 斜距接近

固定 45°相机在目标正上方无法继续看见靶心，因此 EGO 的第一目标点不是靶心上方，而是：

- 高度 1.4 m；
- 水平距离约 1.4 m；
- 相机朝向目标；
- 前方和下降圆柱均无障碍；
- 悬停 0.5 s 后重新验证 PnP。

### 9.2 控制权分配

- FAST-LIO2：持续输出当前机体位姿和速度；
- 视觉：持续更新 `T_map_target` 及相对误差；
- Precision Approach：生成目标参考轨迹；
- EGO/局部碰撞监视：验证轨迹安全；
- 飞控：跟踪位置、速度和姿态指令。

不能表述为“视觉接管后关闭导航”。正确语义是“目标参考由视觉闭环修正，状态估计仍由 LIO 提供”。

### 9.3 下降轨迹

无人机沿斜线降低高度并缩短水平距离，使靶标逐步从画面中心移动到下部：

1. `VISUAL_STANDOFF`：1.4 m 高，目标位于中心可靠 ROI。
2. `VISUAL_DESCENT`：0.2–0.3 m/s，横向误差闭环，目标保持在图像下半部。
3. `EDGE_APPROACH`：目标接近图像下边缘，降低速度至 0.1–0.15 m/s。
4. `EXPECTED_LOSS`：低高度、连续稳定跟踪后从下边缘消失。
5. `LIO_TERMINAL_BRIDGE`：冻结最后可靠世界目标位姿，完成剩余 0.1–0.2 m 水平移动。
6. `DROP_HOVER`：目标上方悬停，满足释放门禁。

建议释放高度：

- 红十字：0.50–0.65 m；
- 标准靶：0.70–0.90 m。

高度的最终选择由落体漂移、下洗气流、机体包络和舵机出口位置试验确定。

## 10. LOST-SIGNAL 与终端桥接

### 10.1 丢失判定

单帧漏检不触发切换。建议满足任一条件才进入 LOST：

- 连续 5 帧无有效目标；
- 无有效观测持续 0.15–0.25 s。

### 10.2 预期丢失

同时满足：

- 丢失前目标位于图像下边缘区域；
- 高度低于类别阈值；
- 最近 0.3 s 跟踪稳定；
- 世界坐标协方差低；
- LIO 健康且下降通道仍为空。

动作：

- 显示/记录 `LOST-SIGNAL: EXPECTED`；
- 保存最后可靠的世界位姿、协方差和时间戳，而不是保存最后像素；
- 冻结目标，不再外推图像测量；
- 执行长度 ≤0.20 m、绝对上限 0.25 m，时间 ≤1.0 s 的专用终端轨迹；
- EGO 仅保留碰撞监视，不重新启动长距离全局规划。

### 10.3 异常丢失

目标从图像中心/侧面消失、高度过高、PnP 突跳或 LIO 退化均属于异常丢失。

动作：

1. 立即停止下降并悬停；
2. 沿最近安全轨迹后退或上升 0.1–0.2 m；
3. 在 2 s 内尝试重新捕获；
4. 两次失败则取消本次接近，将目标退回队列并增加风险代价；
5. 若安全状态不确定，直接升至 1.4 m 或进入返航。

### 10.4 终端桥接禁止条件

- 目标位置标准差超过：标准靶 8 cm、红十字 4 cm；
- 丢失方向不符合下边缘预期；
- LIO 协方差或速度跳变超阈值；
- 终端轨迹穿过未知/占据空间；
- 横滚/俯仰 >5°；
- 剩余水平距离 >0.25 m。

## 11. 三路 PWM 重力投放

### 11.1 软件状态

每个通道独立维护：

`LOADED_ASSUMED → RESERVED → RELEASE_COMMAND → OPEN_HOLD → EMPTY_ASSUMED → RESET_COMMAND → LOCKED_EMPTY`

普通 PWM 舵机无位置、开门和货物离舱反馈，因此不得使用 `OPEN_CONFIRMED` 或 `RELEASE_CONFIRMED`。

### 11.2 每通道参数

```text
PWM_LOCK[3]
PWM_RELEASE[3]
OPEN_HOLD_MS[3] = 800–1000 ms
servo_quality_rank[3]
payload_cg_effect[3]
```

红十字优先分配实测最可靠的通道，但还需考虑释放后的重心变化，不能把通道永久绑定到某类目标。

### 11.3 释放门禁

- 水平误差：标准靶 ≤0.10 m，红十字 ≤0.05 m；
- 水平速度 <0.05 m/s；
- 垂直速度接近 0；
- 横滚/俯仰 <3°；
- 稳定悬停 ≥0.5 s；
- LIO 健康，下降/上升圆柱清空；
- 通道状态为 `RESERVED`，且事务 ID 未执行过。

### 11.4 单次释放事务

1. 创建唯一 `drop_transaction_id`；
2. 将目标先标记为 `ATTEMPTED`，防止重入；
3. 对唯一选中通道发送 `PWM_RELEASE`；
4. 保持 0.8–1.0 s；
5. 通道置为 `EMPTY_ASSUMED`，目标置为 `RELEASED_UNVERIFIED`；
6. 发送 `PWM_LOCK`，通道置为 `LOCKED_EMPTY`；
7. 垂直上升至安全高度，再返回 1.4 m；
8. 不因视觉无法确认落点而重复开门。

全局互锁规定任何时刻只允许一个舵机动作；姿态不稳或安全监督器报警时禁止释放。

### 11.5 机械要求

- 出口净空比货物最大包络每侧至少多 5–10 mm；
- 释放路径无螺钉、台阶和摩擦夹点；
- 断电保持锁止，避免上电抖动开门；
- 舵机供电与主计算机/飞控隔离去耦，动作时电压跌落不能导致重启；
- 装载后做倒置、振动、急停和最大姿态测试。

## 12. 投放后恢复搜索

投放前保存：

- 当前 `segment_id`；
- 沿段进度 `checkpoint_s`；
- 已覆盖栅格版本；
- 当前前进方向。

释放完成后：

1. 沿已验证的上升圆柱升至 1.4 m；
2. 选择原段检查点前方 0.3–0.5 m 的安全重入点；
3. 若原段已被新障碍切断，标记剩余部分为待重规划；
4. 恢复覆盖，不重复统计已观察地面；
5. 更新任务剩余时间和载荷。

## 13. 走廊、两扇门与降落

### 13.1 走廊入口

- 完成投放/触发截止后，规划到走廊入口前 0.8–1.0 m 的等待点；
- 高度降至 1.0–1.2 m；
- 根据 MID-360 点云确认通道方向、宽度和墙面；
- 机头与走廊轴对齐后再前进。

### 13.2 门框估计

1. 在多帧点云中拟合门墙平面；
2. 提取左右门柱边缘，估计门中心和法向；
3. 对中心位置做时间滤波并给出协方差；
4. 用无人机含桨叶/保护圈的真实包络膨胀墙体；
5. 门宽估计不足以容纳膨胀包络加安全余量时，不进入。

### 13.3 穿门轨迹

- 门前等待点距离 0.8–1.0 m；
- 速度 0.2–0.3 m/s；
- 门洞内保持恒定高度和偏航，禁止转弯；
- 门中心偏差阈值由机体宽度确定，建议两侧最小剩余净空各 ≥5 cm；
- 完全越过门后再调整方向；
- 两扇门分别建立独立状态和通过事件。

### 13.4 H 降落

1. YOLO 检测 H 区，几何模块用外框与 H 结构做 PnP；
2. EGO 导航至视觉斜距点；
3. 使用与投放相同的视觉引导下降和 LOST 分类；
4. 预期丢失后短距离 LIO 终端下降；
5. 触地通过高度、垂直速度、加速度和电机状态联合判断；
6. 确认触地后停桨并将任务置为 `COMPLETE`。

降落过程中不执行任何舵机动作。

## 14. 安全监督与故障处置

| 故障 | 检测 | 自动动作 |
|---|---|---|
| FAST-LIO2 未初始化 | 状态无效/协方差高 | 禁止起飞；已飞则悬停后安全降落 |
| FAST-LIO2 短时退化 | 协方差上升、创新异常 | 停止下降/投放，减速悬停，等待恢复 |
| EGO 无可行路径 | 规划超时或轨迹碰撞 | 悬停，扩大局部搜索；两次失败回到上个安全点 |
| 点云延迟 | 帧率/时间戳异常 | 禁止进入未知空间和门洞 |
| YOLO 类别冲突 | 连续投票不一致 | 保持候选，不进入投放 |
| PnP 多解/误差高 | 重投影、尺度、法向不一致 | 后退或换视角重新观测 |
| 重复目标 | 世界门限与轨迹冲突 | 合并记录，保留最小协方差 |
| 异常丢失 | 非下边缘/高空丢失 | 停止下降、后退/上升并重捕获 |
| 终端桥接风险 | 距离或协方差超限 | 禁止盲移，返回视觉斜距点 |
| 舵机状态未知 | 无反馈/动作超时 | 仍标记 `EMPTY_ASSUMED`，目标不重投；赛后检查 |
| 门中心不稳定 | 多帧方差过大 | 门前等待或后退，不强行通过 |
| 电池/时间不足 | 阈值触发 | 跳过低价值任务，立即走廊或安全返航 |
| 可能碰撞 | 距离小于制动距离 | 安全监督器覆盖任务命令，制动/后退 |

任何应急动作都必须遵守规则：正式自主阶段不依赖队员电脑或遥控介入来继续得分。

## 15. 建议接口与数据契约

以下为语义接口，实际 ROS 话题名应与现有工程统一：

| 数据 | 发布者 | 消费者 | 最低内容 |
|---|---|---|---|
| LIO 状态 | FAST-LIO2 | 全部规划/控制 | 位姿、速度、协方差、时间戳、健康 |
| 局部占据图 | 地图模块 | EGO、覆盖、终端、安全 | 占据、未知、分辨率、版本 |
| 目标观测 | target_perception | tracker | 类别、PnP、协方差、质量、图像时间 |
| 世界目标 | tracker | mission/approach | `T_map_target`、状态、质量、更新时间 |
| 搜索段 | coverage_planner | mission/EGO | 端点、进度、覆盖增益、风险 |
| 轨迹状态 | EGO/terminal | mission/safety | 轨迹、碰撞裕度、执行进度 |
| 投放命令 | mission | payload_manager | 事务 ID、目标 ID、通道、PWM |
| 投放状态 | payload_manager | mission/logger | 软件假设状态、开始/结束时间、错误 |
| 安全状态 | safety_supervisor | 全系统 | `OK/WARN/HOLD/ABORT/STOP` 与原因 |

所有跨节点消息必须携带单调时间戳和序列号。投放命令按事务 ID 幂等，重复消息不能触发第二次舵机动作。

## 16. 默认参数基线

| 参数 | 初值 | 说明 |
|---|---:|---|
| 搜索高度 | 1.40 m | 团队决定；低于约 1.5 m 障碍顶部 |
| 航带间距 | 1.10 m | 标定后最大 1.20 m |
| 搜索速度 | 0.50–0.70 m/s | 随图像清晰度调节 |
| 近障碍速度 | 0.25–0.40 m/s | 按制动距离限制 |
| 视觉下降速度 | 0.20–0.30 m/s | 边缘阶段降至 0.10–0.15 |
| 标准靶释放高度 | 0.70–0.90 m | 由试验冻结 |
| 红十字释放高度 | 0.50–0.65 m | 由试验冻结 |
| LOST 连续帧 | 5 帧 | 同时限制 0.15–0.25 s |
| 终端桥接距离 | ≤0.20 m | 绝对上限 0.25 m |
| 终端桥接时间 | ≤1.0 s | 超时立即中止 |
| 投放前悬停 | ≥0.5 s | 速度/姿态同时达标 |
| 舵机开门保持 | 0.8–1.0 s | 每通道独立标定 |
| 门前等待距离 | 0.8–1.0 m | 多帧拟合门中心 |
| 穿门高度 | 1.0–1.2 m | 以实测门/机体包络为准 |
| 穿门速度 | 0.2–0.3 m/s | 门洞内不转弯 |
| 可见性栅格 | 0.10 m | 完成度统计 |
| 覆盖完成率 | ≥98% | 且无大盲区 |
| 走廊+降落保留 | 120–150 s | 加 30 s 安全裕度 |

参数文件应区分 `RULE_FIXED`、`CALIBRATED`、`TUNABLE` 三类；比赛配置禁止在线人工修改。

## 17. 核心决策伪代码

```text
while mission_not_complete:
    update_health()
    update_time_budget()
    update_map_and_visibility()
    ingest_target_observations()

    if safety_requires_abort:
        execute_safe_policy()
        continue

    if state == SEARCH_NAV:
        if must_leave_for_corridor_or_landing():
            state = CORRIDOR_ENTRY
        elif best_drop_candidate_passes_all_gates():
            save_search_checkpoint()
            reserve_target_and_payload()
            state = OPPORTUNITY_DROP
        else:
            execute_next_best_search_segment()

    elif state == OPPORTUNITY_DROP:
        navigate_to_visual_standoff()
        if not revalidate_target_and_corridor():
            cancel_approach_and_resume_search()
        else:
            visual_guided_descent()
            if unexpected_loss:
                hover_backoff_and_reacquire()
            elif expected_loss and terminal_bridge_allowed:
                execute_terminal_bridge()
                if release_gates_pass:
                    execute_one_shot_servo_transaction()
            climb_and_resume_from_checkpoint()

    elif state in CORRIDOR_AND_LANDING_STATES:
        execute_lidar_door_and_visual_landing_logic()
```

## 18. 比赛日操作流程

### 18.1 检录前

- 核对三件货物、舵机锁止、重心和电池；
- 清洁相机镜头与 MID-360 视窗；
- 加载只读比赛参数包，记录 Git 提交号和模型哈希；
- 验证相机分辨率、曝光上限和全局快门；
- 检查飞控、计算机、雷达和舵机供电；
- 执行三通道不带载低幅自检，不实际开门；
- 清空旧地图、旧目标和旧事务 ID。

### 18.2 放机后

- 不依赖人工输入起飞点坐标；
- 确认机体周围和舵机出口无阻挡；
- 向裁判申请起飞前完成所有电脑配置；
- 裁判调整场地后不再进行电脑/遥控操作；
- 只通过规则允许的启动开关触发一次自主任务。

### 18.3 飞行中自动记录

- 当前状态、剩余时间、载荷状态；
- LIO 健康和协方差；
- 覆盖率和最大盲区；
- 每个目标类别、世界坐标、PnP 误差；
- 每次投放的事务 ID、通道和 PWM；
- 门中心、净空和通过事件；
- LOST-SIGNAL 类型和恢复结果。

## 19. 验证、训练与放行标准

### 19.1 台架与标定

- 相机内参重投影误差满足团队阈值，边缘残差单独报告；
- 外参在拆装、撞击和运输后复测；
- 三路舵机各进行 ≥100 次带载循环，无卡滞、误开和计算机掉电；
- 电池最低允许电压下同时测试飞控、雷达、计算机和舵机动作；
- 实物测量所有 PnP 模板点的三维坐标，不从图片比例猜测。

### 19.2 视觉与定位

在高度 1.4、1.0、0.8、0.6 m，多个偏航、自然光/灯光、运动速度下：

- 每类目标检测召回率和误检率；
- PnP 世界位置误差 P50/P95；
- 红十字位置标准差；
- ROI 坐标反变换单元测试；
- IPPE 多解选择正确率；
- 预期/异常丢失分类准确率。

### 19.3 搜索与地图

- 随机起点、随机靶位、随机树和箱位置；
- 10×8 m 测试场与 10×10 m 参数场均不依赖硬编码；
- 有效覆盖率 ≥98%，0.35 m 靶不因遮挡修补遗漏；
- 历史软先验失效时仍能完成覆盖；
- 搜索中插入投放后可正确恢复检查点。

### 19.4 投放

- 标准靶与红十字分别至少 30 次实投；
- 记录不同高度的落点均值、方差和下洗偏差；
- 目标从图像下边缘预期丢失后，终端桥接距离不超限；
- 单次命令重发不会重复开门；
- `ATTEMPTED` 目标不再次进入队列；
- 三通道释放后重心变化仍能稳定上升。

### 19.5 过门与整场

- 两扇门左右位置随机，门宽按约 0.8 m；
- 至少连续 10 次无碰穿过两门；
- LIO、点云或门中心异常时能在门前停止；
- 完整整场演练不少于 10 次，每次在 10 min 内结束；
- 最差耗时演练仍留 ≥30 s 安全裕度；
- 日志能复盘每次决策和规则得分。

### 19.6 正式放行门槛

全部满足才标记 `MATCH_READY`：

- 无 P0/P1 未解决故障；
- 最近 5 次随机全流程至少 4 次完成三投、两门和 H 降落；
- 最近 10 次无撞墙、网和地面；
- 关键参数已冻结并双人复核；
- 领队会歧义项已写入配置和纸质检查表。

## 20. 规则—设计追踪

| 规则要求 | 设计章节 | 验证证据 |
|---|---|---|
| 靶标/障碍随机 | 5、6、7 | 随机起点和随机布置全场测试 |
| 全程自主 | 2、4、18 | 裁判启动后无人工输入日志 |
| 在线避障且非越顶 | 6、13、14 | 1.4 m 搜索、EGO 轨迹、点云回放 |
| 目标识别和投放 | 7–12 | PnP 误差、投放事务、落点统计 |
| 10 min 限时 | 8、16 | 时间截止触发与整场耗时 |
| 两扇门 | 13 | 随机门洞连续通过试验 |
| 自主降落 | 13 | H PnP、终端下降和触地日志 |

## 21. 资料与版本控制

设计依据：

- 用户提供的《2026 中国机器人大赛暨 RoboCup 机器人世界杯中国赛无人机快递赛规则》PDF。
- 用户提供的《RoboCup UAV 2025 实际比赛场地规划》PDF，仅作历史场地与测试搭建参考。
- Livox MID-360 官方产品页与规格页：<https://www.livoxtech.com/mid-360>。
- Livox MID-360 以太网协议：<https://github.com/Livox-SDK/livox_wiki_en/blob/master/source/tutorials/new_product/mid360/livox_eth_protocol_mid360.md>。
- FAST-LIO 官方仓库及 MID-360 示例配置：<https://github.com/hku-mars/FAST_LIO>。

每次模型、标定、参数或机械结构变更必须：

1. 修改版本号；
2. 记录变更原因和影响；
3. 重跑受影响的验收用例；
4. 生成只读比赛包；
5. 保存 Git 提交号、模型哈希和参数哈希。

